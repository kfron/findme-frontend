const env = process.env;

const config = {
    db: {
        user: 'kfron',
        host: 'localhost',
        database: 'findme',
        password: 'password',
        port: 5432
    }
}

module.exports = config;